[homelab_servers]
server1 ansible_host=192.168.68.75 ansible_user=floki ansible_ssh_common_args='-o StrictHostKeyChecking=no'

[raspberry_pi]
# Find Pi IP: check your router DHCP leases or run `arp -n` on the server
# Set a static IP on your router for the Pi's MAC address, then update this
pi1 ansible_host=REPLACE_WITH_PI_IP ansible_user=pi ansible_ssh_common_args='-o StrictHostKeyChecking=no'

[all_nodes:children]
homelab_servers
raspberry_pi
